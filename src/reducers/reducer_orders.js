import { ONE_CLICK_BET } from "../src/actions/index";

const INIT_STATE = {};

export default function (state = INIT_STATE, action) {

  switch (action.type) {
    case ONE_CLICK_BET:
      break;
  }

  return state;
}